import { biosBootSchema } from '../schemas/biosBoot';
import { servicesSchema } from '../schemas/services';
import { projectsSchema } from '../schemas/projects';

export const schemaTypes = [biosBootSchema, servicesSchema, projectsSchema];
